class Solution {
    public void printNumbers(int n) {
        // Base case
        if (n == 0) {
            return;
        }

        // Recursive call
        printNumbers(n - 1);

        // Print after recursion
        System.out.print(n + " ");
    }
}
