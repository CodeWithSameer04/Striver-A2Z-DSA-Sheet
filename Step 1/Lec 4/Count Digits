class Solution {
    public int countDigit(int n) {
        // Handle 0 explicitly
        if (n == 0) return 1;

        n = Math.abs(n); // handle negative numbers
        int count = 0;

        while (n > 0) {
            count++;
            n /= 10;
        }

        return count;
    }
}